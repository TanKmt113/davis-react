# Liên hệ nhanh (floating widget)
VITE_ZALO_URL=https://zalo.me/84969846563
VITE_FACEBOOK_URL=https://www.facebook.com/

# Domain production — dùng cho Open Graph, canonical, sitemap (bắt buộc khi deploy)
VITE_SITE_URL=https://your-domain.com

VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_CHAT_ID=

# Gmail SMTP — chỉ dùng trên Supabase Edge Function (không commit mật khẩu)
SMTP_USER=
SMTP_PASS=
CONTACT_TO=dotrongtan113@gmail.com

# Supabase — server (@supabase/server, Edge Functions, scripts)
# Copy from Supabase Dashboard → Connect dialog. Never commit SUPABASE_SECRET_KEY.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SUPABASE_JWKS_URL=
# Optional: auto-create table (Dashboard → Database → Connection string URI)
SUPABASE_DB_URL=

# Supabase — client (Vite, publishable key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
